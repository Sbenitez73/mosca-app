import 'package:flutter_test/flutter_test.dart';

void main() {
  // Integration tests for Mosca live in test/ when run against a real device.
  // Unit tests for parsers, formatters, and repositories go here.
  test('placeholder', () => expect(true, isTrue));
}
